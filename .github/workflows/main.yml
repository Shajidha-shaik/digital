name: digital application updation.

on: [push]

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Poster Uploaded Successfully
        run: echo "✅ Poster Uploaded Successfully!"
